<form method="post" action="{{ path('app_create_consultant_delete', {'id': consultant.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce consultant ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consultant.id) }}">
    <button class="btn btn-outline-danger mt-2">Supprimer</button>
</form>