{% extends 'header.html.twig' %}

{% block title %}Modifier un consultant{% endblock %}

{% block body %}
{{ include('navbar.html.twig') }}
   <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mt-2">Modifier un consultant</h1>
            </div>
        </div>
    </div>

    <div class="m-5">
        {{ include('admin/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('app_dashboard_admin_index') }}" role="button" class="btn btn-outline-success mt-2">Retour à la liste liste</a>

        {{ include('admin/_delete_form.html.twig') }}
    </div>
{% endblock %}