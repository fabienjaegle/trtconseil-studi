{% extends 'header.html.twig' %}

{% block title %}Nouveau consultant{% endblock %}

{% block body %}
{{ include('navbar.html.twig') }}

<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header text-center mt-3">Créer un nouveau consultant</h1>
        </div>
    </div>
    
    <div class="m-5">
        {{ include('admin/_form.html.twig') }}

        <a href="{{ path('app_dashboard_admin_index') }}" role="button" class="btn btn-outline-success mt-3">Retour à la liste</a>
    </div>    
</div>
{% endblock %}